/**
 * Test stub for Nitro's `#nitro-internal-virtual/server-handlers-meta` virtual
 * module so unit tests can import server utils that reach it (via
 * `server/utils/route-required-roles.ts`). Aliased in `vitest.config.ts`. The
 * route-role matching logic itself is unit-tested directly in
 * `tests/unit/route-roles-match.test.ts` with synthetic metas.
 */
export const handlersMeta: unknown[] = [];
