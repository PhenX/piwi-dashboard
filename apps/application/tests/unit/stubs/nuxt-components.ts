/**
 * Test stub for Nuxt's `#components` virtual module so unit tests can import
 * pure helpers from modules that reference auto-imported components (e.g.
 * `app/utils/index.ts` imports `UIcon` for `createSortHeader`). Aliased in
 * `vitest.config.ts`. Only the identifiers the unit suite touches need to exist.
 */
export const UIcon = { name: 'UIcon' };
